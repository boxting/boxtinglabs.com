# PostHog project API key (client-side, public). Copy to .env and fill in.
# Get it from: PostHog → Project settings → Project API key (starts with phc_)
PUBLIC_POSTHOG_KEY=
# Ingestion host. Swap for a reverse-proxy domain later if needed.
PUBLIC_POSTHOG_HOST=https://us.i.posthog.com
